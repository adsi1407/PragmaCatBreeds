# This file configures the analyzer, which statically analyzes Dart code to
# check for errors, warnings, and lints.
#
# The issues identified by the analyzer are surfaced in the UI of Dart-enabled
# IDEs (https://dart.dev/tools#ides-and-editors). The analyzer can also be
# invoked from the command line by running `flutter analyze`.

# The following line activates a set of recommended lints for Flutter apps,
# packages, and plugins designed to encourage good coding practices.
include: package:very_good_analysis/analysis_options.yaml

analyzer:
  exclude:
    - build/**
    - "**/*.g.dart"
    - "**/*.freezed.dart"

linter:
  # The lint rules applied to this project can be customized in the
  # section below to disable rules from the `package:very_good_analysis/analysis_options.yaml`
  # included above or to enable additional rules. A list of all available lints
  # and their documentation is published at https://dart.dev/lints.
  #
  # Instead of disabling a lint rule for the entire project in the
  # section below, it can also be suppressed for a single line of code
  # or a specific dart file by using the `// ignore: name_of_lint` and
  # `// ignore_for_file: name_of_lint` syntax on the line or in the file
  # producing the lint.
  rules:
    # Documentation - Disabled for better developer experience
    public_member_api_docs: false  # Too verbose for internal projects
    
    # Code quality and complexity metrics (enhanced from very_good_analysis)
    avoid_classes_with_only_static_members: true
    avoid_function_literals_in_foreach_calls: true
    prefer_const_constructors: true
    prefer_const_declarations: true
    prefer_final_locals: true
    prefer_single_quotes: true
    avoid_redundant_argument_values: true
    
    # Method and class design patterns
    avoid_returning_this: true
    cascade_invocations: true
    avoid_setters_without_getters: true
    prefer_final_parameters: true
    
    # Performance and best practices
    avoid_slow_async_io: true
    avoid_dynamic_calls: true
    unnecessary_await_in_return: true
    prefer_if_null_operators: true
    prefer_null_aware_operators: true
    unnecessary_null_checks: true
    
    # Type safety and null safety
    cast_nullable_to_non_nullable: true
    omit_local_variable_types: true
    avoid_init_to_null: true
    unnecessary_nullable_for_final_variable_declarations: true
    
    # Readability and formatting
    lines_longer_than_80_chars: false  # Disabled - modern monitors support longer lines
    eol_at_end_of_file: true
    sort_constructors_first: true
    sort_unnamed_constructors_first: true
    directives_ordering: true
    prefer_adjacent_string_concatenation: true
    prefer_interpolation_to_compose_strings: true
    
    # Code organization and structure
    sort_pub_dependencies: true
    prefer_relative_imports: false  # Prefer package imports for better maintainability
    always_use_package_imports: true
    
    # Error handling and control flow
    avoid_catches_without_on_clauses: true
    avoid_empty_else: true
    control_flow_in_finally: true
    
    # Collections and iterables
    prefer_is_empty: true
    prefer_is_not_empty: true
    prefer_contains: true

    
    # Function and method design
    prefer_function_declarations_over_variables: true
    avoid_positional_boolean_parameters: true
    prefer_expression_function_bodies: false  # Can reduce readability for complex logic

# Additional information about this file can be found at
# https://dart.dev/guides/language/analysis-options
